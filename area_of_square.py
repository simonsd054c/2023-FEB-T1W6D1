# Type casting
# Get length from user
length = int(input("Enter the length: "))

# Calculate and store area in a variable using the formula: length * length
area = length * length

# print the calculated area to the console
# print(f"The area of the square with length {length} is {area}")

print("The area of the square with length " + str(length) + " is " + str(area))